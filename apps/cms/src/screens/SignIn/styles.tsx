import styled from 'styled-components';
import { CenterWrapperView } from '@lava-x/components';

export const StyledCenterViewWrapper = styled(CenterWrapperView)`
  .logo-brand {
    margin-left: auto;
    margin-right: auto;
  }
`;
